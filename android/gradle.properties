# AndroidX (required — Compose, WorkManager, DataStore, Convex SDK all use AndroidX)
android.useAndroidX=true

# Gradle JVM
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.parallel=true

# Kotlin
kotlin.code.style=official
